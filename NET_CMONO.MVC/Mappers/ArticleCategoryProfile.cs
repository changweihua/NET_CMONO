using AutoMapper;

namespace NET_CMONO.MVC.Mappers
{
    public class ArticleCategoryProfile : Profile, IProfile
    {
        public ArticleCategoryProfile()
        {
            // CreateMap<User, UserDto>();
            // CreateMap<UserDto, User>();
        }
    }
}